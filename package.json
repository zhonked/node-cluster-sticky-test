{
  "name": "node-cluster-sticky-test",
  "version": "0.0.1",
  "description": "Testing sticky sessions via node's cluster module",
  "main": "sticky-cluster-test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zhonked",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.0",
    "socket.io": "^1.3.5"
  }
}
